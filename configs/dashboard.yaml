# Dashboard configuration

dashboard:
  title: "BESS Digital Twin Dashboard"
  port: 8501
  
  plots:
    enable_soc_timeline: true
    enable_voltage_profile: true
    enable_current_profile: true
    enable_pca: true
    enable_comparison: true
    
  refresh_interval: 5  # seconds
  
  theme: "light"  # or "dark"
